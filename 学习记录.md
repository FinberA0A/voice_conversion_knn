***第一课***：
傅里叶变换告诉我们：任何信号都能分解成不同频率正弦波的叠加。但如果对整段 8 秒直接做一次傅里叶变换，你得到的是"这 8 秒里总共有哪些频率成分"——时间信息全部丢失，你不知道某个频率是什么时候出现的。这对语音毫无用处，因为语音的全部意义就在于频率成分随时间的变化模式。

"短时"就是解决这个矛盾的办法：把信号切成一小段一小段，每段单独做傅里叶变换，再按时间顺序排成一张二维图。横轴时间、纵轴频率、颜色是能量强度——这就是第二张子图。

Sampling rate表示对一段声音的采样的频率，即例如将1s中划分为16000份，每一次选取1/16000为一格来察看振幅之类的信息

对于不同的声音的波形，由于振幅不同，那么进行normalize是非常有必要的，保证后续数据的处理

梅尔谱：由于人对不同频段的声音的敏感度不同，人相较于高频段来说对低频段的声音更加敏感，梅尔谱就是将高频段的进行相较于低频段来说更强的压缩。（梅尔谱同时带着音色和说的话的信息）

f0是基频(foundamental frequency)，用于表示声音的音高，可以用于说话人音高迁移等操作

丢掉相位，那么各个谐波的相位就无法保持一致，导致听起来像是金属声

关于tensor就是pytorch中的多维矩阵，形如[[1],[2]]这样是[1*2的二维]、形如[1,2]这样是[一条向量]

**在声音转换过程中**
为什么是 WavLM 而不是梅尔谱？（）
    梅尔谱里内容和音色是纠缠的——同一个音素，你说和角色说，梅尔谱长得很不一样。
    WavLM 这类自监督模型经过大规模无标注语音的掩码预测训练，中间层表示主要由
    "发的是什么音"决定，说话人信息被大幅削弱。这正是我们做检索匹配需要的性质。（同一句话不同人在WavLM上的第六层的特征是相近的）

为什么固定取第 6 层？
    自监督模型的层有明显分工：浅层偏声学细节（含大量说话人信息），
    深层偏语义/上下文，中间层音素信息最纯。knn-vc 原论文做了逐层实验，
    第 6 层在"内容保持"和"音色剥离"之间最平衡，其配套的 HiFi-GAN 声码器
    也是在第 6 层特征上训练的——层号必须和声码器一致，改了就得重训声码器。
（浅层更多关注说的话的信息，第六层存在恰好的平衡）

prematched 权重是在"kNN 平均之后"的特征上微调的。

    这是个典型的训练/推理分布失配问题：kNN 取均值会让特征比原始特征更平滑，
    如果声码器只见过原始特征，遇到平滑特征就会产生沙哑和杂音。用 prematched
    版本能明显提升清晰度——记住这个思路，你以后调任何两阶段系统都会用到。

***问题：关于dropout\batchNorm\反向传播更新权重\神经网络batch

经过通读代码文件和增加target数据扩充特征池，发现新增的数据质量较低，严重影响了knn-vc的效果
所以决定换更优质的语音材料

***更换使用LJspeech的英文演讲素材
消融实验：
topk(表示选取的较近特征向量的个数) 
1       听感上感觉更柔和了？和readme上写的预测不同
4       比topk=1的时候音色要准一点，但是中间有些断断续续的
20      更多的机器音，挺硬的
100     更多的电流音
3       增加了一些电流音
2       比topk=3的时候电流音少

layer(神经网络的不同层蕴含的信息不同，浅层更多是说话人的信息如音色等)
1       有一种女性声音和男性声音揉在一起的感觉，效果很差，但是能分清说的是什么
6       经典的默认第六层
12      声音更加被破坏，断裂感增加，远远不如layer6, 稍逊layer1
8       更多的混乱的感觉，偏向target的口音
4       听感很好，比layer6要更舒服一些（可能是因为这个声音更贴近自己的声音）
5       和使用layer4一样好

raw_vocoder
除去    感觉没什么太大差异，声音变得更生硬了一些

语言更换
英文    效果非常好，即使本人素材录得较为一般，但是output非常顺畅，没什么违和感

- **音高跟随源说话人**。这个方法不显式建模 F0，男声转女性角色会有违和感。
  想解决就要引入 F0 条件建模，那已经是 RVC / so-vits-svc 的范畴了。
- **需要目标角色 5 分钟以上素材**，做不到几秒钟的 zero-shot。
- **跨语言效果差**，源和目标最好是同一语言。

***第二课***：
使用正规且直观的标准（f0_corr|speaker_cosine|cer）来进行转换后声音的评估
参考区间: f0_corr >0.8 良好 | speaker_cosine >0.6 音色到位 | cer <0.15 内容清晰
topk 2      layer 4:
=== 评估结果 ===
             f0_corr: 0.2754
    f0_rmse_semitone: 10.2255
      speaker_cosine: 0.5597
                 cer: 0.8163

topk 2      layer 5:
=== 评估结果 ===
             f0_corr: 0.3586
    f0_rmse_semitone: 9.1853
      speaker_cosine: 0.5095
                 cer: 0.7959
(转化后得到的结果较为准确)

topk 2      layer 6:
=== 评估结果 ===
             f0_corr: 0.1947
    f0_rmse_semitone: 11.0345
      speaker_cosine: 0.2937
                 cer: 0.8571

topk 4      layer 6:
=== 评估结果 ===
             f0_corr: 0.6456
    f0_rmse_semitone: 9.8468
      speaker_cosine: 0.4647
                 cer: 0.8163
（转化后的结果较为准确）
这三者就是此消彼长的，在指标下还是默认的topk4 layer6更加好

topk 1      layer 6:
=== 评估结果 ===
             f0_corr: 0.7192
    f0_rmse_semitone: 10.1698
      speaker_cosine: 0.4351
                 cer: 1.0000
（topk取1的时候得到的f0很好，speaker_cosine也没有太低，
内容虽然不太清晰，但是比多topk的得到的f0的提升多了很多）
**小结论**
1、topk本来是用来给找不到精准匹配的源特征的用来平均补偿的；但是在扩大特征池检索的时候，音素覆盖的就越来越广
就能够检索到几乎完全匹配的近邻，所以就不需要平均的策略了

2、对于knn-vc这种没有训练的、仅仅利用数据固有特性的非参数方法，一定要保证数据的质量（不像RVC这种训练模型有容错性，能够削弱坏数据的影响）

***第二课下午***
进行CNN复习，CNN利用卷积技术，使用卷积核进行原始数据的压缩与信息的提取；关键信息的提取如下：
**卷积核**：卷积核只是一个工具，用来方便提取原始数据中的信息
**步长（stride）**：是指卷积核滑动的幅度（**实际上是整个数据的缩放幅度**，比如对一维数据的处理，步长为2，则最后处理的数据中元素的个数就为原来的一半；步长为3，那么同理是元素个数变为原来的1/3）
**感受野RF**：表示当前经过压缩后的元素涵盖了原始数据中的多少个元素，具体计算公式=上一层感受野+（本层kernal-1）*上层累计的下采样倍率（**实际上是步长的累乘**）
（感受野递推：新感受野 = 旧感受野 + (kernel-1) * 之前所有stride的乘积）

在进行消融实验或者说是其他测试的时候，一定要注意维护一个json文件来记录训练的各项参数，如下：
{
  "source": "data/source/meEnglish.wav",
  "target_dir": "data/target/LJSpeech",
  "topk": 4,
  "layer": 6,
  "prematched": true,
  "out": "outputs/converted2topk4layer.wav",
  "f0_corr": 0.6456,
  "speaker_cosine": 0.4647,
  "cer": 0.8163
}

因为好像无法在HiFi-GAN的内部增加调整音高的部分，现在只能使用后处理的方式，即在HiFi-GAN对特征向量进行上采样得到波形之后再进行音高的调整；


***第三课***
topk取2，layer取6时的f0虽然有差距（男女声源的差距限制了），
所以尝试使用调整f0的工具再声码器后进行后处理，但是发现必须牺牲一些音色来调整f0
整体的效果反倒不如原来的t2_l6,**正因如此，才会有人将f0作为声码器的条件输入**

***第四课***
使用rvc工具进行wav的转化，得到的wav文件经过metric.py的评测，得到的spk_completion终于得到了突破，达到0.917
spk_converted也达到了0.817（ceiling 0.888）
(所以并不是原来的数据有问题，只是knn-vc这个朴素的方法达到了顶点了)
(后续会进行相应的RVC的内容的学习：VITS)

***第五课***
问题：需要实际上去研读rvc的python代码的时候发现对于python语法还有一些生疏————尤其是有关tensor的计算的部分，关于维度和运算方面