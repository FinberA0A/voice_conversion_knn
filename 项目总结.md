# 声音转换项目总结

从零手写 knn-vc 到用 RVC 突破其上限的完整记录。本文只收录**在干净实验条件下**
得到的结论；`学习记录.md` 保留原始的学习过程和当时的判断，两份都别删——
错误的中间记录是这个项目最值钱的部分。

---

## 一、最终成果

用同一批 13 分钟 LJSpeech 数据、同一段 16 秒源音频、同一套自建评估脚本横向对比：

| 方案 | spk_completion | wer | 音高可控 |
|---|---|---|---|
| knn-vc（手写，零训练） | 0.712 | 0.032 | 否，由检索决定 |
| knn-vc + WORLD 后处理 | 0.586 | 0.032 | 是，代价 −0.126 |
| **RVC (if_f0=0)** | **0.917** | **0.000** | 否，由模型决定 |
| RVC (if_f0=1) | 待测 | 待测 | 是，预期零代价 |

`spk_completion = (转换后 - 源) / (目标自身 - 源)`，0 表示白转，1 表示达到目标
说话人自身水平。参照点：源基线 0.033，目标上界 0.888。

**核心结论：0.71 是 kNN 检索的结构性上限**，不是数据量或超参数问题——换层号、
换 topk、池子扩三倍，它都纹丝不动。kNN 只能从池子里挑真实存在的帧；RVC 学的是
一个映射，能生成训练集里从未出现过的帧组合。这 0.2 的差距就是"检索"和
"学习映射"的分野。

但要诚实：RVC 的优势不全来自架构。`pretrained_v2` 底模在大量多说话人数据上
预训练过，13 分钟数据做的只是微调。**效果差异是架构 + 预训练 + 数据三者的
共同结果**，把功劳全归给架构是常见误判。

---

## 二、技术路线与每一步撞的墙

```
① 手写 knn-vc
   WavLM(第6层) → kNN 检索替换 → HiFi-GAN
   墙：音高完全不可控，输出被搬到目标音域 +7 个半音，无从干预
        ↓
② WORLD 后处理控制 F0
   把第一课的源-滤波器模型写成代码，拆出 f0/sp/ap，只改 f0 再合成
   墙：WORLD 基于经典信号处理假设，而输入是 HiFi-GAN 生成的波形，
       假设失配导致包络估计有损 —— 音色税 0.126，不可优化
        ↓
③ 理解 NSF 为何存在
   音高必须在合成过程中作为条件注入，而非事后修正。
   代价是声码器要重训 —— 这正是 ② 做不到的事
        ↓
④ RVC
   ContentVec → (+F0 embedding) → VITS(VAE+Flow) → NSF-HiFiGAN
   外挂 faiss 检索（就是 ① 里手写的那个 kNN）
```

**② 是全程最有价值的一步。**它是个负结果，但正因为亲手交过那 0.126 的学费，
才真正理解 NSF 把 F0 做成条件输入是在解决什么问题——而不是从论文里读到
"所以要用 NSF"。

---

## 三、干净的实验数据

### knn-vc 层号消融
条件：`complicateSentenceVocals.wav` / 39277 帧池 / topk=2，三份 JSON 的
`source_sha1` 与 `matching_set_frames` 完全一致。

| layer | f0_corr | spk_completion | wer |
|---|---|---|---|
| 4 | **0.890** | 0.699 | 0.097 |
| 5 | 0.858 | 0.713 | 0.097 |
| 6 | 0.715 | 0.712 | **0.032** |

- **音色**：三层无差异（极差 0.014，噪声）。说话人信息在第 4 层就已剥离干净。
- **韵律**：随层号单调流失。这是往深走的真实代价，建立在 400+ 个浊音帧上，可信。
- **内容**：layer 6 更好，但差异只来自 "nine one one" 一处，样本量为 1，仅是线索。

**层号的本质是"保留多少声学信息"的旋钮。**往浅调，韵律跟得紧但音色剥离不净；
往深调，音色干净但韵律失控。音高本身就是一种声学信息。

### F0 后处理消融（layer 6）

| 模式 | f0_corr | spk_completion | 说明 |
|---|---|---|---|
| off | 0.715 | 0.712 | 不走 WORLD |
| roundtrip | 0.564 | 0.621 | 只走 WORLD，F0 一个字节不改 |
| contour | 0.798 | 0.586 | 源语调 + 目标音域 |

拆开看：往返本身 f0 −0.151 / 音色 −0.092（纯损失，零收益）；改 F0 则
f0 **+0.234** / 音色 −0.034（在控制往返变量后，contour 是有效的）。

⚠️ 这三组的 f0_corr 用 pyin 测量，与处理端的 harvest 不一致，存在约 0.15 的
工具偏差。已修（`metrics.py` 默认改为 harvest），但**尚未用统一工具重测**。

---

## 四、已作废的早期结论

这些出现在 `学习记录.md` 里，是脏实验的产物。保留原文，但别再引用。

| 已作废的说法 | 实际情况 |
|---|---|
| "音高跟随源说话人" | 反了。音高被搬向目标（+7 半音），且不可控 |
| topk / layer 的主观听感（第 42~63 行） | 那批实验中途换过源音频，两个变量同时变，作废 |
| "raw vocoder 差异不大" | 同上，源不一致 |
| 第二课那批评估数字（f0_corr 0.19~0.72 / cer 0.8+） | 带噪源 + 文本正规化 bug（把 `9-1-1` 和 `911` 归一到不同形式），全部作废 |
| "layer 6 音高失控，0.47 vs 0.85" | 幅度被脏源夸大近一倍。干净条件下是 0.715 vs 0.890，趋势成立但没那么夸张 |
| "浅层更多关注说的话的信息" | 写反了。浅层含说话人信息，中间层音素最纯（已在笔记中修正） |

**保留有效的**：第一课全部声学理解、CNN 与感受野推导、小结论 1（池子越大最优
topk 越小）、小结论 2（非参数方法对数据质量极度敏感）、第三课关于 F0 条件输入的结论。

---

## 五、预测 vs 实际

每个实验前先下预测再验证，这是全程最重要的习惯。五次预测，两中三错：

| 预测 | 实际 | 得到的教训 |
|---|---|---|
| 层号越深，音色越像目标 | 第 4~6 层无差异 | 小样本上的趋势多半是噪声 |
| 池子扩到 15 分钟会明显提升音色 | 0.715 → 0.712，纹丝不动 | 论文说的 5~10 分钟饱和点是对的 |
| contour 会让 f0_corr 上到 0.95+ | 0.798 | 对照组选错（该和 roundtrip 比而非 off），且测量链有噪声底 |
| roundtrip 的 spk 落在 0.58~0.62 | 0.621 | ✅ |
| RVC 能突破 0.71 | 0.917 | ✅ |

**预测错了不要紧，没有验证机制才要紧。**三次错误各自暴露了一个方法论漏洞，
补上之后才有了第三节那张干净的表。

---

## 六、方法论十条

这是本项目真正的产出，全部来自踩坑。

1. **先确认实验干净，再看结果。**每个输出旁边写一份参数快照（源文件 sha1、
   特征池帧数、所有超参）。因为缺记录，白白浪费过两轮实验。
2. **指标必须有参照点。**孤立的 `speaker_cosine=0.61` 毫无意义，必须同时给出
   不转换的下界和目标自身的上界。
3. **指标必须可审计。**永远打印 ASR 的原始转写和逐词差异明细。曾经
   `wer` 相同的两组，一组是真实音素错误，另一组只是 `9-1-1` 写成了 `911`。
4. **对照组只能差一个变量。**拿 `off` 和 `contour` 比是错的，它们差了两个变量，
   导致真实效果（+0.234）被掩盖成看不出来（+0.083）。
5. **测量链的工具必须统一。**处理端用 harvest、评估端用 pyin，凭空引入约 0.15
   的 f0_corr 偏差——大到足以淹没真实效果。
6. **不同指标的统计可靠性差几个数量级。**`f0_corr` 背后有 400+ 个观测，`wer`
   的差异可能只来自一处替换。前者可谨慎相信，后者只是线索。
7. **先下预测再验证。**事后解释永远能自圆其说，事前预测才能暴露认知漏洞。
8. **负结果同样有价值。**"扩池无用"省下了继续堆数据的时间；"后处理有音色税"
   直接解释了 NSF 的设计动机。
9. **非参数方法对数据质量极度敏感。**训练型模型能靠梯度平均掉坏数据；kNN 的
   池子里每一帧脏数据都是随时会被检索到的地雷。质量远比数量重要。
10. **人耳和模型测量的不是同一个东西。**WORLD 往返在听感上几乎无差别，却让
    `spk_completion` 掉了 0.09。两者都是真的，只是关注的维度不同。

---

## 七、知识地图

### 声学
- 采样率与奈奎斯特定理；语音 16k、歌声 44.1k
- 准平稳假设 → 分帧加窗 → STFT；25ms 窗 / 20ms 跳步
- 梅尔谱：对数频率感知，80 维压缩，**丢弃相位**
- **源-滤波器模型**：语音 = 声源激励 ⊛ 声道响应
  - 声源 = F0 = 音高；声道 = 共振峰 = 音色
  - 内容藏在共振峰的**时间轨迹**里，音色藏在其**绝对位置**上
- 倒谱法分离包络与谐波；F0 提取（pyin / harvest / RMVPE / CREPE）
- log 域做音高运算，因为音高感知是对数的

### 模型
- **WavLM-Large**（315M）：7 层 CNN 前端总步长 320、感受野 400 点（≈ 可学习的
  STFT）+ 24 层 Transformer。无 padding 卷积导致 1 秒出 49 帧而非 50 帧
- **层的分工**：浅层含说话人信息，中间层音素最纯，深层偏语义
- **HiFi-GAN**（16.5M）：上采样 [10,8,2,2] 连乘 320，与 WavLM 严格互逆；
  MRF 多感受野融合；MPD 按质数周期折叠捕捉谐波、MSD 多尺度
- 为什么合成波形要用 GAN：相位在感知上不重要但数值上极敏感，L1 回归会得到
  平均化的模糊结果
- **NSF**：按 F0 生成正弦激励注入声码器内部，一次成型
- **VITS**：VAE 解决一对多（同样内容对应无数合法波形）、Flow 增强先验表达力
  （可逆 ⇒ 概率密度可精确计算 ⇒ KL 可直接优化）、GAN 补最后的波形保真度
- **训练/推理行为不同的三个例子**：dropout 关闭、BatchNorm 切滑动平均、
  weight norm 合并。凡是为帮助优化而引入的结构，推理时都要关掉或折叠

---

## 八、未完成的实验

按价值排序：

1. **RVC 重训 `if_f0=1`**。填上第一节表格的最后一行。预期 `spk_completion`
   保持 0.9 上下，`f0_corr` 跃升到 0.9+，且**不付音色代价**——这是整条学习
   路径要证明的论点。
2. **用统一的 harvest 重测 F0 后处理三组**，把第三节那个 ⚠️ 去掉。
3. **用 3 段不同风格素材重复 layer 消融**，确认 f0_corr 的单调趋势稳定。
   单次实验的模式再漂亮也只是线索。
4. **RVC 的 `index_rate` 消融**。它控制"原始特征"与"检索特征"的插值比例，
   调高即趋近手写的 knn-vc。这是把两个方法连起来的最直接实验。
5. **回到最初的目标**：换动漫角色干音，做一个能拿出手的成品。

---

## 九、延伸关键词

声学：`Nyquist theorem` `source-filter model` `formant` `cepstral liftering`
`mel filterbank` `griffin-lim phase reconstruction`

表征：`HuBERT` `WavLM` `ContentVec speaker disentanglement`
`layer-wise probing of speech SSL`

合成：`HiFi-GAN MPD MSD` `neural source filter NSF` `WORLD vocoder CheapTrick D4C`
`VITS normalizing flow` `variational inference TTS`

方法：`kNN-VC` `RVC retrieval based voice conversion` `faiss ANN`
`speaker verification ECAPA-TDNN` `mel cepstral distortion`

---

## 十、伦理

声音属于人格权益，《民法典》第 1023 条对自然人声音有保护规定。本项目仅用于
非商业的个人学习实验：只使用本人声音或已获公开授权的数据集（LJSpeech 为
公有领域），产出内容注明 AI 合成，不发布可能被误认为真人发言的音频。
