# 声音转换学习规划 · v2

> v1 作废。它假设"knn-vc 是你手写的，所以 tensor 操作已经熟了"，
> 于是把语法训练当成补漏项。实际情况是：**代码能力是主线，其余全部排在它后面。**

---

## 先把账算清：你手里真正有什么

被 AI 代写的是**代码**，不是**理解**。这两样要分开清算，否则你会误判自己的起点。

### 真的属于你的（不用重做）

| 资产 | 为什么它是真的 |
|---|---|
| 声学理解 | 采样率、STFT、梅尔谱、源-滤波器模型、共振峰 vs F0、相位丢失 —— 这些是你看图、听声、反复追问建立起来的，AI 写代码不会替你长出这些直觉 |
| 实验设计能力 | 层号消融、topk 消融、F0 后处理三组对照，**变量是你选的，对照组是你设计的** |
| 结果解读能力 | "0.71 是 kNN 检索的结构性上限"这个判断，是你在扩池无效、换层无效之后自己收敛出来的。这是研究能力，不是编程能力 |
| 方法论十条 | 全部来自你亲手踩的坑。参照点、单变量、测量链统一、先预测再验证 —— 很多读研两年的人没建立起这套 |
| 负结果的价值判断 | 认定"WORLD 后处理的 0.126 音色税"是理解 NSF 的入口，而不是一次失败 |

**这些是研究者的核心能力，而且比编程能力更难获得。** 不要因为代码是 AI 写的就否定它们。

### 缺的那一块

**从空文件写出能跑的 PyTorch 代码。** 具体是三层：

1. Python 本身：类、装饰器、上下文管理器、argparse、路径与 IO
2. NumPy / Tensor：形状、广播、索引、维度约定 —— 你自己在第五课点名了这一块
3. PyTorch 训练：`nn.Module`、autograd、优化器、训练循环、device/dtype

**这一块补不上，后面全都是黑盒。** 读 RVC 源码读不懂，训练调参只能按按钮，
VITS 的数学永远停在"能复述结论"。所以 v2 把它提到最前面，而且给了它足够的时间。

---

## AI 使用规约（这份规划成立的前提）

上一轮的教训不是"不该用 AI"，而是**没有区分"用 AI 产出结果"和"用 AI 建立能力"**。
两个目标要用两套规则。

### 学习阶段（第 0~2 阶段全程适用）

**允许**

- 解释概念："广播规则到底怎么对齐的"
- 解释报错：把完整报错贴给 AI 问原因
- **写完之后**让 AI review：指出你的实现哪里有 bug、哪里不地道
- 给关键词和文档指路："算余弦距离矩阵该用哪个 API"
- 帮你设计验证方法："我怎么确认这个函数写对了"

**禁止**

- 让 AI 写你正在学的那个模块 —— 一行都不行
- 让 AI 补全整个函数体，包括 Copilot 的整块 ghost text（建议这两个月关掉行内补全，只留手动触发）
- **复制粘贴任何超过 3 行、你不能逐行讲出作用的代码**
- 卡住不到 20 分钟就问 AI。先自己查文档、打印形状、二分定位

### 判断"真的会了"的两个测试

- **白纸测试**：关掉全部参考和编辑器，在空文件里重写这个模块的核心 10 行。写不出来就没会。
- **讲解测试**：对着代码逐行讲出"这一行为什么必须存在、删掉会怎样"。
  讲不出来的行，就是你抄的行。

### 产出阶段（第 6 阶段之后）

规则放开。工程师用 AI 提速是常态，前提是**你有能力判断它写得对不对**。
第 0~2 阶段就是在换取这个判断力。

---

## 时间预算

| 阶段 | 内容 | 周期 |
|---|---|---|
| −1 | 能力基线自测 | 半天 |
| 0 | Python / NumPy / PyTorch 基本功 | 3~4 周 |
| 1 | **从零重写 knn-vc（不用 AI）** | 2~3 周 |
| 2 | ML 基础 + 层探针实验 | 2 周 |
| 3 | RVC 实验闭合 + 源码解剖 | 2~3 周 |
| 4 | VITS 数学三块 | 3 周 |
| 5 | F0 与 NSF | 1 周 |
| 6 | 训练动态与效果归因 | 2 周 |
| 7 | 成品 | 1~2 周 |

合计约 4 个月。**前两个阶段占了五分之一多的时间却不产出任何新音频**，
这是必须接受的代价 —— 它买回来的是后面每一个阶段的真实性。

---

# 阶段 −1 · 能力基线自测

**半天。** 不做这一步，整份规划都是猜的。

规则：**关掉 AI、关掉搜索、关掉本仓库所有代码**，只允许查官方文档。
每题限时 10 分钟，写在 `drills/00_baseline.py` 里。

| # | 题目 | 考什么 |
|---|---|---|
| 1 | 读一个 wav 成 numpy 数组，打印形状、采样率、时长秒数 | 基础 IO |
| 2 | 把一个 `[T, D]` 的 numpy 数组转成 tensor 并搬到 GPU，再搬回 numpy | dtype / device |
| 3 | **手写余弦相似度矩阵**：输入 `[A, D]` 和 `[B, D]`，输出 `[A, B]`，不许用现成的距离函数 | 归一化 + 矩阵乘 + 形状 |
| 4 | 用 `argparse` 写一个接受 3 个参数（含一个带默认值）的命令行脚本 | Python 工程 |
| 5 | 定义一个 `nn.Module` 子类，内含一个 `Linear`，`forward` 返回 sigmoid 输出 | nn.Module 结构 |
| 6 | 写一个完整训练循环：前向、算 loss、`zero_grad`、`backward`、`step`，跑 10 步打印 loss | autograd 与优化器 |
| 7 | 把一个 10000 行的矩阵按每 512 行一块分块处理，结果拼回原形状 | 分块与拼接 |
| 8 | 把一个字典写成 JSON 文件，再读回来 | 文件 IO |
| 9 | 给第 3 题的函数加类型注解和 docstring，标出输入输出形状 | 代码规范 |
| 10 | 用 matplotlib 画两条曲线在同一张图上，加图例，存成 png | 可视化 |

**计分与分流**

| 独立写出的题数 | 起点 |
|---|---|
| 0~3 | 从 0-A 开始，全部关卡按顺序做，预算取 4 周 |
| 4~6 | 从 0-B 开始，0-A 当复习快速过一遍，预算 3 周 |
| 7~9 | 从 0-C 开始，重点补 0-C 和 0-D，预算 2 周 |
| 10 | 直接进阶段 1，但阶段 1 的规矩一条都不能松 |

**把结果写进 `学习记录.md`。** 这是你的第一个诚实基线，
和你给 knn-vc 记的"源基线 0.033"是同一种东西 —— 没有基线就无法衡量进步。

---

# 阶段 0 · Python / NumPy / PyTorch 基本功

**3~4 周。目标不是"看懂"，是"能从空文件写出来"。**

统一做法，每一关都是三步：

```
读官方文档 → 在空文件里写出来 → 用 assert 验证，不许用眼睛核对 print
```

`assert x.shape == (2, 3, 4)` 会在你想错的地方立刻炸掉；`print` 靠你自己核对，
而你正是那个还不熟的人。每关一个文件放在 `drills/`。

## 0-A · Python 语言基础（3~5 天）

**练什么**

- 函数：位置参数 / 关键字参数 / 默认值 / `*args` / `**kwargs`
- 类：`__init__`、`self`、方法、继承与覆写、`super()`
- 装饰器：先理解"函数是对象"，再写一个打印耗时的 `@timer`
- 上下文管理器：`with` 做了什么，用 `contextlib` 写一个
- 路径与 IO：`pathlib.Path`、glob、JSON 读写、Windows 路径转义
- `argparse`：这是你后面每个实验脚本的入口
- 列表推导 / 生成器 / `enumerate` / `zip` / f-string
- 类型注解：`list[str]`、`str | None`、注解**不会**被运行时检查

**产出**：`drills/01_python.py`。里面必须有一个自定义装饰器、一个自定义上下文管理器、
一个带继承的类、一个能跑的 argparse 入口。

**白纸测试**：空文件写出一个 `@timer` 装饰器。

## 0-B · NumPy 与形状思维（4~6 天，本阶段最关键）

这一关直接对应你第五课点名的痛点。**先建立形状记账习惯，再学 API。**

**形状记账法**：以后你写的每一行数组操作，后面跟一句形状注释。
全程统一符号，不许换：`B` batch、`D` 特征维、`T` 帧数、`L` 采样点、`C` 通道、`H` 头。

```python
wav = librosa.load(path, sr=16000)[0]   # [L]          L = 16000 * 秒数
feats = extractor(wav)                  # [T, 1024]    T ≈ L / 320
dists = cosine_distance(feats, pool)    # [T, N]       N = 池子帧数
```

**练什么**

- 创建与形状：`zeros` / `ones` / `arange` / `reshape` / `shape` / `ndim`
- 增删维度：`None` 索引、`expand_dims`、`squeeze`
- **广播规则**：从最后一维往前对齐，维度相等或其中一个为 1 才能广播
- 切片与负索引、`...`（Ellipsis）
- **花式索引**：用整数数组索引另一个数组，为什么会凭空多出一维
- 按轴归约：`sum` / `mean` / `max` 的 `axis` 与 `keepdims`
- 矩阵乘：`*` 逐元素、`@` 矩阵乘、`einsum`
- 布尔掩码与条件赋值：`x[x > 0] = 1`、`np.where`

**必做的形状推导题**（先手推，再写脚本验证）

设 `A` 形状 `[100, 8]`、`B` 形状 `[5000, 768]`、`ix` 是取值在 `[0, 5000)` 的整数数组形状 `[100, 8]`。

1. `B[ix]` 的形状是什么？为什么？
2. `A.sum(axis=1)` 和 `A.sum(axis=1, keepdims=True)` 分别是什么形状？
3. `B[ix] * A[..., None]` 能广播吗？结果形状？
4. 上一题的结果 `.sum(axis=1)` 是什么形状？
5. 如果第 3 题写成 `B[ix] * A`，会报错还是静默算错？

这五题不是我编的 —— 它就是 RVC 里 faiss 加权检索那四行的骨架，阶段 3 会正面遇到。

**产出**：`drills/02_numpy.py`，五道推导题各配一个 `assert`。

**白纸测试**：口述广播规则，然后写出"给 `[T, 8, 768]` 按第 1 轴做加权平均、权重形状 `[T, 8]`"。

## 0-C · PyTorch Tensor（4~6 天）

**练什么**

- tensor 与 ndarray 互转，共享内存的坑（`from_numpy` vs `tensor`）
- `dtype`：float32 / float16 / long，什么时候必须是 long（索引、Embedding 输入）
- `device`：`.to()`、`.cpu()`、"expected all tensors on the same device" 怎么排查
- **两套维度约定**：卷积与插值要 `[B, C, T]`，Transformer 与 Linear 要 `[B, T, C]`。
  **PyTorch 代码里满地的 `permute` 全是在这两者之间倒换** —— 认清这一点，
  RVC 源码的可读性会立刻提升一半
- `permute` / `transpose` / `view` / `reshape` / `contiguous` 的区别
- `topk` / `sort` / `gather` / `cat` / `stack` / `chunk`
- `F.normalize`、`F.interpolate`、`F.pad`
- 就地操作 `_` 后缀的风险

**必做的报错收集**（故意制造，记下完整报错原文，存成 `drills/errors.md`）

1. `permute` 之后调 `view` —— 记下那句 non-contiguous 报错，以后你会见很多次
2. CPU tensor 和 GPU tensor 相加
3. float 张量当索引用
4. 形状不匹配的广播失败
5. 对 `requires_grad=True` 的张量调 `.numpy()`

**认熟报错比记住 API 更省时间。** 报错是你未来两年最主要的信息来源。

**产出**：`drills/03_tensor.py` + `drills/errors.md`。

**白纸测试**：写出"把 `[B, T, C]` 的张量在时间轴上插值成 2 倍长度再变回 `[B, T, C]`"。
这一行就是 `rvc/infer/vc/pipeline.py:198`。

## 0-D · PyTorch 训练机制（5~7 天）

不用真实数据，用 `y = 3x + 2` 加噪声这种玩具任务，把机制跑通。

**练什么**

- `nn.Module`：`__init__` 里注册子模块、`forward`、为什么调 `model(x)` 而不是 `model.forward(x)`
- `nn.ModuleList` vs 普通 list —— **故意用普通 list 装子模块，
  然后打印 `len(list(model.parameters()))` 看到 0。这个坑值一整天调试时间，现在花 5 分钟买掉**
- autograd：`requires_grad`、`backward()`、`.grad`、`detach()`、`torch.no_grad()`
- 训练循环五步：前向 → loss → `zero_grad` → `backward` → `step`。
  **漏掉 `zero_grad` 会怎样？亲手试一次，看 loss 曲线变成什么形状**
- 优化器：SGD / Adam / AdamW，`lr` 调大调小分别怎么坏
- `Dataset` / `DataLoader` / batch / `collate_fn`（变长序列必须自己写 collate）
- `model.train()` vs `model.eval()`：Dropout 和 BatchNorm 的行为差异，**亲眼看到同一输入两次输出不同**
- 存取权重：`state_dict` / `load_state_dict`

**手推一次梯度（不许跳）**

用 numpy 手写 logistic regression 的梯度并训练收敛，再换 autograd 实现，
两者的梯度值 `np.allclose` 对比。公式就三行，但**跳过这一步，
阶段 4 的 VAE 梯度你永远只能"相信它"**。

**产出**：`drills/04_train.py`，含 numpy 版和 autograd 版两个实现。

**白纸测试**：空文件写出完整训练循环，包括 `zero_grad` 的位置。

## 0-E · 音频处理库（2~3 天）

- `librosa`：`load`（`sr` / `mono` 参数）、`stft`、`melspectrogram`、`pyin`
- `soundfile`：读写 wav，和 librosa 的分工
- `numpy` 音频惯例：float32、范围 `[-1, 1]`、单声道 `[L]` vs 多声道 `[L, 2]`
- 采样率转换、归一化

**产出**：`drills/05_audio.py` —— 读一个 wav，画出波形 + 频谱图 + F0 曲线三张子图。
写完之后再打开 `explore/01_acoustics.py` 对比，看 AI 当初怎么写的、你的版本差在哪。

**这是阶段 0 到阶段 1 的过渡**：第一次"先自己写、再和参考实现对比"。

---

# 阶段 1 · 从零重写 knn-vc

**2~3 周。这是整份规划的枢纽阶段。**

## 为什么必须做这件事

- 算法你**真的懂**（原理、层号、topk、prematched 权重的动机都能讲清）
- 规模合适：`knnvc/` 只有 290 行，`eval/metrics.py` 223 行
- **有参考实现可对答案**，但更硬的是：**有数字可复现**
- 一次把"理解"转成"能力"，且不需要学任何新概念 —— 认知负荷全部花在写代码上

**验收标准不是"能跑"，是"复现 `项目总结.md` 第三节的数字"**：
topk=2 / layer=6 条件下，`spk_completion ≈ 0.712`、`wer ≈ 0.032`。
数字对不上就是有 bug，这比任何主观判断都硬。

## 操作规矩

1. 新建 `myvc/` 包，你的代码全写在这里。**`knnvc/` 原样保留**，它是基线，
   也是最后的对照物。
2. **写一个模块的过程中，不许打开 `knnvc/` 对应的文件。** 写完、跑通、
   自测过了，才允许打开做 diff。
3. 每个模块我只给**接口签名和输入输出形状**，不给实现思路。
4. 卡住的处理顺序：打印形状 → 查官方文档 → 二分注释掉代码定位 →
   **满 20 分钟仍无进展**，才允许问 AI，且只能问概念，不能要代码。
5. 每个模块完成后写三行记录进 `学习记录.md`：卡在哪、怎么解决的、diff 后发现参考实现哪里更好。

## 模块拆解（按难度排序，严格顺序做）

### 1-1 · `myvc/io.py`（半天，热身）

```python
def load_wav(path: str, sr: int = 16000) -> np.ndarray:
    """读单声道 wav，返回 [L] 的 float32，范围 [-1, 1]。"""

def save_wav(path: str, wav: np.ndarray, sr: int = 16000) -> None: ...

def sha1_of_file(path: str) -> str:
    """参数快照要用它记录源文件身份。"""
```

### 1-2 · `myvc/features.py`（2~3 天）

```python
class FeatureExtractor:
    """WavLM 帧级特征抽取。"""

    def __init__(self, layer: int = 6, device: str = "cuda") -> None:
        """用 torch.hub.load("bshall/knn-vc", "wavlm_large") 拿模型。"""

    def from_array(self, wav: np.ndarray) -> torch.Tensor:
        """[L] -> [T, 1024]，T ≈ L / 320，帧率 50Hz。"""

    def from_file(self, path: str) -> torch.Tensor: ...

    def build_pool(self, audio_dir: str) -> torch.Tensor:
        """遍历目录下所有音频，抽特征后拼成 [N, 1024] 的检索池。"""
```

**必须自己解决的三个问题**

1. `torch.hub.load` 怎么调用、参数是什么、权重缓存在哪
2. **输入波形要不要归一化？** WavLM 训练时做了实例归一化，
   推理不一致会怎样？（`model.cfg.normalize` 这个字段是干什么的）
3. `extract_features` 怎么指定层号，返回值是什么结构

**自测**：喂 1 秒 16k 音频，输出帧数应该是 49 而不是 50。
**说出为什么是 49** —— 你在总结第七节写过答案（无 padding 卷积），
现在要在自己的代码里验证它。

**加分项**：加特征池缓存。抽 15 分钟音频要几分钟，做消融时池子不变，
每次重抽纯属浪费。缓存键要含层号和文件清单指纹，否则换层会拿到陈旧的池子。

### 1-3 · `myvc/matching.py`（2~3 天，**核心**）

```python
def cosine_distance(query: torch.Tensor, pool: torch.Tensor) -> torch.Tensor:
    """[Tq, D] x [Tp, D] -> [Tq, Tp]，返回距离（1 - 余弦相似度）。"""

def knn_convert(query: torch.Tensor, pool: torch.Tensor,
                topk: int = 4, chunk: int = 512,
                device: str = "cuda") -> torch.Tensor:
    """把源特征逐帧替换为池中 topk 近邻的均值。[Tq, D] -> [Tq, D]"""
```

**这是整个 knn-vc 唯一的"模型"，也是阶段 0-B/0-C 全部形状训练的靶子。**

**必须自己解决的四个问题**

1. 为什么用余弦而不是欧氏距离？（提示：WavLM 特征的模长编码什么）
2. **为什么必须分块？** 算一下：15 分钟池子约 45000 帧、源音频 30 秒约 1500 帧，
   距离矩阵 `[1500, 45000]` 的 float32 占多少显存？源音频换成 5 分钟呢？
3. `topk` 取最小还是最大？`torch.topk` 的 `largest` 参数怎么设？
4. `pool[best.indices]` 是什么形状？为什么 `.mean(dim=1)` 而不是 `dim=0`？

**自测（不需要跑完整流程）**

- 构造 `[10, 4]` 的 query 和 `[100, 4]` 的 pool，验证输出形状 `[10, 4]`
- **令 pool 的前 10 行就等于 query**，那么 `topk=1` 的输出必须等于 query 本身。
  这是一个能自己造出来的单元测试，比听音频可靠得多
- 把 `chunk` 从 512 改成 7，输出必须逐元素相同（分块不能改变结果）

**加分项**：打印 top1 距离的中位数和 90 分位。这个数字是"池子够不够用"的诊断指标 ——
偏大说明目标池里缺对应音素，此时调任何超参都是徒劳。这是你自己总结出的结论，
现在把它做成工具。

### 1-4 · `myvc/vocoder.py`（1~2 天）

```python
def load_vocoder(device: str = "cuda", prematched: bool = True): ...

def synthesize(vocoder, feats: torch.Tensor) -> np.ndarray:
    """[T, 1024] -> [L]，L ≈ T * 320。"""
```

**必须自己解决**

1. `prematched` 权重和普通权重差在哪？为什么默认要用前者？
   （你在第一课记过：训练/推理分布失配。现在在代码里指出这个开关在哪一行）
2. 输入形状是 `[T, D]` 还是 `[B, T, D]`？需要 `unsqueeze` 吗？

**自测**：`--raw-vocoder` 用非 prematched 权重跑一次，应该听到沙哑和杂音。
**这是"训练/推理分布失配"的听觉证据**，从概念变成了你亲耳听到的东西。

### 1-5 · `myvc/cli.py`（1 天）

`argparse` 入口，串起前四个模块。参数：`--source` / `--target-dir` /
`--topk` / `--layer` / `--out` / `--raw-vocoder`。

**硬性要求：每次运行必须落一份参数快照 JSON**，含
`source_sha1`、`pool_frames`、全部超参。格式沿用你原来那套。
这是你方法论第 1 条，因为缺记录白白浪费过两轮实验 —— 现在它必须由你亲手实现。

### 1-6 · `myvc/metrics.py`（3~4 天，最大的一个）

```python
def f0_consistency(src: str, conv: str) -> dict:
    """F0 相关系数与半音 RMSE。工具统一用 harvest（pyworld）。"""

def speaker_similarity(src: str, conv: str, target_dir: str) -> dict:
    """ECAPA 嵌入余弦相似度，必须同时给出三个点：
       源基线（下界）、转换结果、目标自身（上界）。
       spk_completion = (转换 - 源) / (目标 - 源)
    """

def content_preservation(src: str, conv: str) -> dict:
    """faster-whisper 转写算 WER。必须打印原始转写和逐词差异明细。"""
```

**这个模块是你方法论的代码化**，三条硬要求都来自你自己踩的坑：

- 指标必须有参照点 → `speaker_similarity` 返回三个数而不是一个
- 指标必须可审计 → WER 必须打印逐词 diff，因为你曾被 `9-1-1` / `911` 骗过
- 测量链工具统一 → F0 一律 harvest，因为混用 pyin 引入过 0.15 的偏差

依赖要装：`faster-whisper`、`speechbrain`、`jiwer`（`requirements.txt` 里注释着的三行）。

## 阶段 1 验收（四条全过才算完成）

1. `python -m myvc.cli --source ... --target-dir ... --topk 2 --layer 6` 能跑通
2. `spk_completion` 复现到 0.712 ± 0.02，`wer` 复现到 0.032 附近
3. 打开 `knnvc/` 逐文件 diff，**列出参考实现里每一处你没想到的细节**，
   并说明它为什么需要（不是"抄下来"，是"解释它"）
4. **白纸测试**：空文件重写 `cosine_distance` 和 `knn_convert` 的核心十行

## 预测 / 实际 / 教训

| 模块 | 预测（写之前：会卡在哪？要几天？） | 实际 | 教训 |
|---|---|---|---|
| 1-1 io | | | |
| 1-2 features | | | |
| 1-3 matching | | | |
| 1-4 vocoder | | | |
| 1-5 cli | | | |
| 1-6 metrics | | | |

---

# 阶段 2 · ML 基础 + 层探针实验

**2 周。** 阶段 0-D 打通了机制，这一阶段用一个真实任务把它坐实，
同时填掉 `学习记录.md` 第 37 行标记的那个空洞。

## 实验 2-1：WavLM 逐层说话人探针

你手里有 `essays/layerWise.pdf`，也有"浅层含说话人信息、中间层音素最纯"这个
**从论文读来的**结论（第 28 行还留着写反了的版本）。现在用你自己的代码验证它。

- **数据**：你的录音帧 vs LJSpeech 帧，WavLM 第 1~24 层各抽一份（复用 `myvc/features.py`）
- **任务**：二分类"这一帧是谁说的"
- **模型**：先 logistic regression，再加一层隐层变 MLP
- **产出**：`explore/04_layer_probe.py` + 24 点折线图（横轴层号、纵轴验证准确率）

**这个实验会让你踩到一个真实的坑**：同一句话的相邻帧高度相关，
如果按帧随机切分 train/val，验证准确率会虚高。
**故意先按帧切分跑一次，记下数字，再改成按句子切分。** 这就是数据泄漏，
比任何教科书定义都记得牢。

顺带把这些从"背过"变成"见过"：train/val 划分、过拟合、Dropout 与 BatchNorm 的
train/eval 行为差异、SGD → Adam → AdamW 的收敛差异（RVC 用的就是 AdamW，
`betas=(0.8, 0.99)`）。

## 自检问题

- 说话人信息峰值在第几层？跨过第几层掉到接近 50%（随机水平）？
- 按帧切分和按句子切分，准确率差多少？为什么这是数据泄漏？
- `model.eval()` 具体改变了哪几类层的行为？在你探针模型的哪一行生效？
- 探针准确率高，能证明"这一层含说话人信息"吗？探针本身的容量是不是混淆变量？
  （**这是个真问题，不是修辞** —— 线性探针和 MLP 探针的结论如果不一致，说明什么？）

## 预测 / 实际 / 教训

| 编号 | 预测 | 实际 | 教训 |
|---|---|---|---|
| 2-1 峰值层号 | | | |
| 2-1 按帧 vs 按句差值 | | | |
| 2-1 线性 vs MLP 探针 | | | |

---

# 阶段 3 · RVC 实验闭合 + 源码解剖

**2~3 周。** 到这里你才具备读 RVC 源码的能力。

## 3-A：补完未完成的实验

| 编号 | 实验 | 唯一变量 | 产出 |
|---|---|---|---|
| 3-1 | RVC 重训 `if_f0=1` | `if_f0`，f0 方法 `rmvpe`，其余与 `Gu` 完全一致（40 epoch / batch 4 / `pretrained_v2` G40k） | 命名 `Gu_f0`，填上总结第一节表格最后一行 |
| 3-2 | `index_rate` 消融 | 0 / 0.25 / 0.5 / 0.75 / 1.0 | 5 行指标 + 一张曲线 |
| 3-3 | F0 后处理三组用 harvest 重测 | 测量工具统一 | 去掉总结第三节那个 ⚠️ |

`logs/Gu` 这次的配置已从 `train.log` 第一行确认：`if_f0=0`、40 epoch、batch 4。
**注意你之前记的"RVC 突破 0.917"用的是无 F0 版本**，`if_f0=1` 仍然没测过。

RVC 实验一律照 knn-vc 的规格落参数快照 JSON。WebUI 不会替你记，
建议走 `rvc/infer/cli.py` 命令行：

```json
{
  "model": "Gu_f0", "source_sha1": "...",
  "if_f0": 1, "f0_method": "rmvpe", "total_epoch": 40, "batch_size": 4,
  "pretrain": "pretrained_v2/f0G40k.pth",
  "index_rate": 0.75, "protect": 0.33, "f0_up_key": 0,
  "spk_completion": null, "f0_corr": null, "wer": null
}
```

## 3-B：`index_rate` 就是你自己写的那个 kNN

阶段 3 的核心阅读是 `rvc/infer/vc/pipeline.py:175~198`：

```python
# rvc/infer/vc/pipeline.py:186~189
score, ix = index.search(npy, k=8)
weight = np.square(1 / score)
weight /= weight.sum(axis=1, keepdims=True)
npy = np.sum(index_vectors[ix] * np.expand_dims(weight, axis=2), axis=1)
```

**这四行就是你在 1-3 亲手写过的 `knn_convert`**，差别只有三点：
topk 固定为 8、用距离平方倒数做加权平均（你写的是等权）、
外面多了一个 `index_rate` 线性插值旋钮。

阶段 0-B 那五道形状推导题的答案在这里对上号（`ix` → `[T, 8]`、
`index_vectors[ix]` → `[T, 8, 768]`、`expand_dims(weight, 2)` → `[T, 8, 1]`、
广播相乘 → `[T, 8, 768]`、`sum(axis=1)` → `[T, 768]`）。

**动手任务**：把 `myvc/matching.py` 的等权平均改成距离加权，形状不许变。
然后回答：`index_rate=1` 趋近纯检索（你的 knn-vc），`index_rate=0` 是纯学习映射，
那么你在 knn-vc 上得到的"池子越大最优 topk 越小"，在这里能否复现？

## 3-C：源码解剖，逐级打印形状

写 `explore/05_rvc_anatomy.py`，在推理链路上逐级打印张量形状。
用阶段 0-B 的形状记账法，做到不查代码也能说出每一步。

| knn-vc（你现在真的懂了） | RVC | 去哪看 |
|---|---|---|
| WavLM-Large 第 6 层，1024 维 | ContentVec（HuBERT-base 结构）最后一层，768 维 | `infer/hubert.py`、`logs/Gu/3_feature768` |
| 50 帧/秒（步长 320 @16k） | 50 帧/秒 → `interpolate(scale_factor=2)` → 100 帧/秒 | `pipeline.py:198` |
| HiFi-GAN 上采样 [10,8,2,2] = 320 | NSF-HiFiGAN 上采样 [10,10,2,2] = **400** = `hop_length` @40k = 10ms | `logs/Gu/config.json` |
| 无 F0 | F0 → 梅尔刻度 → 量化 256 档 → `nn.Embedding(256, 192)` | `pipeline.py:132~138`、`models.py:40` |
| 特征直接送声码器 | 特征 → TextEncoder(6 层 Transformer) → Flow 逆变换 → NSF 声码器 | `models.py:530~737` |

**这一行就是你 0-C 白纸测试写过的那一行**：

```python
# rvc/infer/vc/pipeline.py:198
feats = F.interpolate(feats.permute(0, 2, 1), scale_factor=2).permute(0, 2, 1)
```

清音保护那段是 0-B/0-C 布尔掩码训练的靶子：

```python
# rvc/infer/vc/pipeline.py:211~216
if protect < 0.5 and pitch is not None and pitchf is not None:
    pitchff = pitchf.clone()
    pitchff[pitchf > 0] = 1
    pitchff[pitchf < 1] = protect
    pitchff = pitchff.unsqueeze(-1)
    feats = feats * pitchff + feats0 * (1 - pitchff)
```

**注意第 213 行用 `pitchf` 判断而第 212 行改的是 `pitchff`。**
如果误写成 `pitchff[pitchff < 1] = protect` 会怎样？（`pitchff > 0` 已被置 1 了）

## 自检问题（先猜再验证）

1. 为什么 F0 要先转梅尔刻度再均匀量化，而不是直接按 Hz 均匀量化？
   （和你第一课"log 域做音高运算"是同一件事）
2. 为什么 ContentVec 取最后一层，而 knn-vc 取第 6 层？两个模型的训练目标差在哪？
3. `config.json` 里 `spk_embed_dim: 109` 是什么？你训练时 `sid=0`，另外 108 个去哪了？
4. `protect` 保护的到底是什么？为什么清音帧要特殊处理？
5. `n_mel_channels: 125` —— 为什么不是 knn-vc 那边熟悉的 80？
6. `segment_size: 12800` 除以 `hop_length=400` 等于多少帧？这个数字决定了什么？

## 同期任务

开 TensorBoard 读 `logs/Gu/events.out.tfevents.*`，看四条曲线
`loss/g/mel`、`loss/g/kl`、`loss/g/fm`、`loss/d/total`，**截图存档**。
这一阶段不要求解释，阶段 4 回来逐项拆。

```python
# rvc/train/train.py:524
loss_gen_all = loss_gen + loss_fm + loss_mel + loss_kl
```

## 预测 / 实际 / 教训

| 编号 | 预测 | 实际 | 教训 |
|---|---|---|---|
| 3-1 | | | |
| 3-2 | | | |
| 3-3 | | | |

---

# 阶段 4 · VITS 数学三块

**3 周，全程最重的一段。** 验收线：能对着 `train.py:524` 那一行，
把四项 loss 分别对应到 VAE / GAN / GAN / VAE，
并说清 `c_mel=45` 与 `c_kl=1` 为什么差 45 倍。

每块走三步：**读公式 → 做玩具实验（自己写代码）→ 回 RVC 代码找对应行**。
玩具实验必须自己写 —— 阶段 0 和 1 就是为了这一刻。

## 4a · VAE 与 ELBO

- **读**：Kingma & Welling 原文第 2 节，或 Doersch 的《Tutorial on VAE》
- **推**：`ELBO = 重构项 − KL 项`；为什么"一对多"（同样内容对应无数合法波形）需要隐变量；
  重参数化技巧手推一次梯度
- **写**：在 80 维梅尔帧上训一个小 VAE，观察重构**模糊**。
  这直接对应你已理解的"L1 回归得到平均化结果"，也就是 GAN 项存在的动机
- **回代码**：`models.py:136` `PosteriorEncoder` 就是 q(z|谱)，输出 `m, logs`；
  `train.py:521` 的 `kl_loss(z_p, logs_q, m_p, logs_p, z_mask)` 就是那个 KL

## 4b · Normalizing Flow

- **读**：RealNVP（affine coupling）一篇足够，Glow 选读。核心一句话：
  **可逆变换 + 雅可比行列式好算 ⇒ 密度可精确计算 ⇒ KL 可直接优化**
- **写**：2D 双月数据集上手写 4 层 affine coupling flow，把高斯变成双月并画出来。
  `log_det` 必须自己写，不许调库
- **回代码**：`models.py:82` `ResidualCouplingBlock`，`n_flows=4`。
  训练时 `flow(z)` 正向、推理时 `flow(z_p, reverse=True)` 逆向 ——
  **这就是 VITS 为什么非要可逆结构**

## 4c · GAN 训练动态

你已经懂 MPD/MSD 的结构和动机，缺的是训练过程本身。

- **读**：`rvc/train/losses.py` 全文（很短），LSGAN 损失、feature matching 损失
- **回代码**：`train.py:506~526`，一个 step 里 D 先更新再 G 更新；
  `GradScaler` + `autocast` 是 fp16 混合精度（`train.log` 第一行那句"精度 torch.float16"）
- **观察**：Gu 的曲线里 `loss/d/total` 和 `loss/g/total` 是否在某区间拉锯

## 自检问题

- 四项 loss 各自优化什么？去掉任意一项会坏在哪？
- `c_mel=45` 而 `c_kl=1`，这个倍数说明设计者更在意什么？
- **如果 D 的 loss 掉到接近 0，意味着什么？此时 G 还能学到东西吗？**
- feature matching 损失为什么能稳定 GAN 训练？
- 为什么算 mel 和 kl 损失时要 `autocast(enabled=False)` 关掉 fp16？
  （提示：`kl_loss` 里有 `exp` 和除法）
- `train.py:541~544` 把 `loss_mel > 75`、`loss_kl > 9` 截断 ——
  这是在改梯度还是只改日志？看清它在 `backward` 的前面还是后面

## 预测 / 实际 / 教训

| 编号 | 预测 | 实际 | 教训 |
|---|---|---|---|
| 4a 小 VAE 重构效果 | | | |
| 4b flow 玩具是否收敛 | | | |
| 4c D-loss 趋势 | | | |

---

# 阶段 5 · F0 与 NSF

**1 周。** 把第一课的源-滤波器模型走到底 —— 亲耳听到"声源"在神经网络里的字面实现。

## 5-1：F0 估计器横评

pyin / harvest / rmvpe / fcpe（后两个仓库里有权重和代码），
同一段音频跑四条 F0 曲线叠在一张图上。

总结第三节那个约 0.15 的"工具偏差"，究竟哪个工具更接近真值？
**用一段已知音高的合成正弦波或 vibrato 扫频做 ground truth** —— 没有裁判就没有答案。

## 5-2：把 NSF 的激励信号抽出来听

读 `models.py:267` `SineGen` 和 `models.py:346` `SourceModuleHnNSF`：
按 F0 生成基频 + 谐波的正弦叠加，清音段换成噪声。

**把激励信号单独存成 wav 播放**，你会听到一个嗡嗡的蜂鸣。
这就是第一课"声源激励"的字面实现，声码器剩下的全部工作就是给它套上"声道"。

## 5-3：`Gu` vs `Gu_f0` 的结构差异定位

两者唯一的结构差异：TextEncoder 多了 `emb_pitch`（`models.py:40`、`models.py:61`），
Generator 换成 `GeneratorNSF`（`models.py:403`）。
阶段 3 测到的全部指标差异都源于这两处。在代码里逐行确认这句话是否成立。

## 自检问题

- NSF 把 F0 做成条件输入，和你第三课 WORLD 后处理的做法，本质差别是什么？
  （你已付过 0.126 的音色税，现在要能在代码里指出钱花在哪）
- `SineGen` 为什么要生成谐波而不只是基频正弦？
- 清音段为什么必须换成噪声？（和 `protect` 是同一问题的两面）
- `f0_up_key` 在 pipeline 的哪一步生效？改的是 `f0_coarse` 还是 `pitchf`，还是两者？

## 预测 / 实际 / 教训

| 编号 | 预测 | 实际 | 教训 |
|---|---|---|---|
| 5-1 哪个 F0 工具最准 | | | |
| 5-2 激励信号听起来像什么 | | | |

---

# 阶段 6 · 训练动态与效果归因

**2 周，主要是 GPU 时间。** 回答你自己在总结第一节提出的那个诚实问题：
RVC 的 0.917 里，架构、预训练底模、数据各占多少功劳？

| 编号 | 实验 | 变量 | 要回答什么 |
|---|---|---|---|
| 6-1 | epoch 消融 | 40 / 100 / 200 / 400，每 20 epoch 存权重 | 会不会过拟合？过拟合的 VC 模型听起来什么样？ |
| 6-2 | **从零训练 vs 底模微调** | 不加载 `pretrained_v2`，其余全同 | 13 分钟数据从零训能否到 0.7？如果不能，说明 knn-vc 的 0.71 其实不差 |
| 6-3 | 数据量 | 3 / 13 / 40 分钟（LJSpeech 有 24 小时） | 微调模式的饱和点，和 knn-vc 的 5~10 分钟一致吗？ |
| 6-4 | batch_size | 4 / 8 / 16 | 显存与收敛速度权衡；lr 要不要随 batch 同步调 |

**6-2 是核心** —— 它是唯一能把"架构贡献"和"预训练贡献"分开的实验。

**产出**：一张能直接放进 `项目总结.md` 第一节的新表格。

## 自检问题

- 一个过拟合的 VC 模型，在客观指标和听感上分别有什么特征？
- 为什么 `epochs` 配置写着 20000 而实际训 40 就停？
  （看 `train.py` 里 `total_epoch` 与 `hps.train.epochs` 的关系）
- 6-3 如果饱和点和 knn-vc 一致，说明什么？差很多又说明什么？

## 预测 / 实际 / 教训

| 编号 | 预测 | 实际 | 教训 |
|---|---|---|---|
| 6-1 | | | |
| 6-2 | | | |
| 6-3 | | | |
| 6-4 | | | |

---

# 阶段 7 · 成品

换动漫角色干音，做一个能拿出手的东西。这一阶段处理真实数据管线，
工具仓库里都有：

- `assets/pymss_weights/` 的 BS-RoFormer 做人声分离、
  `dereverb_mel_band_roformer` 去混响 —— 解决 README 里说的"带 BGM / 混响重"素材问题
- 推理参数 `f0_up_key`（跨性别 ±12 半音）、`protect`、`rms_mix_rate`、`index_rate`
  **全部用阶段 3~5 建立的理解去设置，不许照抄默认值**
- 数据清洗沿用 knn-vc 时期的经验：剔除带 BGM、战斗音效、重混响、喊叫哭腔的片段

**从这一阶段起，AI 使用规约放开** —— 你已经有能力判断它写得对不对了。

**伦理约束（沿用总结第十节）**：只用本人声音或已获公开授权素材；
产出注明 AI 合成；不发布可能被误认为真人发言的音频。

---

# 之后往哪走

阶段 7 完成后你会亲身感到 RVC 的短板：**每个目标说话人都要单独训一个模型。**

下一代方向是 **zero-shot VC**（FreeVC / seed-vc / CosyVoice 这一系），
核心从"为每个说话人拟合一个映射"变成"把说话人编码成一个向量当条件"；
以及 **扩散模型路线**（DDSP-SVC / Diff-SVC）。

前置知识 —— 说话人编码器（你 1-6 里用的 ECAPA 就是一个）和条件生成 ——
在阶段 2 和阶段 4 都会碰到。**现在不要碰**，基础没扎实之前它们只会又变成黑盒。

---

# 全程纪律

1. **阶段 0~2 严守 AI 使用规约。** 这是 v2 相对 v1 唯一真正新增的东西，
   也是唯一决定这份规划有没有用的东西。
2. 实验前写预测，实验后填"实际"和"教训"。你五次预测两中三错，
   每次错误都补上了一个方法论漏洞 —— 这个机制是你最好的资产，不能断。
3. 每个 RVC 实验都有参数快照 JSON。
4. **模型命名带变量**：`Gu`、`Gu_f0`、`Gu_f0_scratch`、`Gu_f0_e200`。
   不许再出现两个只有你自己记得区别的模型。
5. 每行 tensor 操作跟一句形状注释。
6. 每个阶段结束写一段"我能回答的问题 / 我还不能回答的问题"，
   最低线是各阶段的自检问题列表。
7. `项目总结.md` 只放干净结论，脏实验留 `学习记录.md`，两份都不删。
8. **`学习记录.md` 里补一句说明：`knnvc/`、`eval/`、`explore/` 是 AI 辅助生成的，
   `myvc/` 是自己写的。** 理由和你保留错误记录的理由完全一样 ——
   记录的价值在于诚实。

# 进度勾选

- [ ] 阶段 −1 · 能力基线自测（结果写进学习记录）
- [ ] 0-A Python 语言基础
- [ ] 0-B NumPy 与形状思维
- [ ] 0-C PyTorch Tensor
- [ ] 0-D PyTorch 训练机制
- [ ] 0-E 音频处理库
- [ ] 1-1 io · 1-2 features · 1-3 matching
- [ ] 1-4 vocoder · 1-5 cli · 1-6 metrics
- [ ] **阶段 1 验收：复现 spk_completion 0.712**
- [ ] 阶段 2 · 层探针实验
- [ ] 阶段 3 · RVC 实验闭合 + 源码解剖
- [ ] 阶段 4 · VITS 数学（4a VAE / 4b Flow / 4c GAN）
- [ ] 阶段 5 · F0 与 NSF
- [ ] 阶段 6 · 训练动态与归因
- [ ] 阶段 7 · 成品
