# 声音转换学习规划 · knn-vc 之后

面向"已经手写完 knn-vc、正在接触 RVC"这个起点。三份文档的分工：

- `学习记录.md` —— 逐日原始笔记，含被推翻的判断，刻意保留
- `项目总结.md` —— 只收录干净实验条件下的结论
- `学习规划.md`（本文）—— 往下走的路线、每阶段的实验设计与自检线

## 怎么用这份文件

1. **每个实验开始前，把预测写进本文对应的空表格**，做完填"实际"和"教训"。
   你的五次预测两中三错，每次错误都补上了一个方法论漏洞，这个机制不能断。
2. **自检问题不许查答案再回答**。先自己写一段，再去代码/论文里验证。
   写不出来说明这一阶段没过，不要往下走。
3. 语法关卡（第零章）不集中做，按各阶段标注的挂钩顺序穿插做。
4. 阶段可以延期，顺序不要调。阶段 3 的数学依赖阶段 1 的梯度直觉，
   阶段 5 的结论依赖阶段 0 的干净对照。

---

# 零、Python / Tensor 语法训练轨

> 起因（`学习记录.md` 第五课）：研读 RVC 代码时发现 Python 语法生疏，
> 尤其是 tensor 计算里的维度问题。

**这一章不刷 LeetCode，也不看语法教程。** 全部练习题取自本仓库真实代码，
做法统一是三步：

```
读真实代码 → 用笔手推每一步的形状 → 写一个最小脚本验证，形状不符就查为什么
```

建议新建 `drills/` 目录，每关一个文件（`drills/t1_broadcast.py` 等），
脚本里大量用 `assert x.shape == (...)`，而不是 `print` 完用眼睛看。
`assert` 会在你想错的地方立刻炸掉，这比自己核对可靠。

## T0 · 形状记账法（半天，最重要的一关）

看不懂 tensor 代码的根因几乎从来不是语法，而是**没有记账习惯**。
以后你写的每一行 tensor 操作，后面都跟一句形状注释：

```python
feats = feats.view(1, -1)                    # [1, L]  L = 采样点数
feats = extract_hubert_features(...)         # [1, T, 768]  T ≈ L/320
feats = feats.permute(0, 2, 1)               # [1, 768, T]
```

统一符号，全程别换：`B` batch、`D` 特征维、`T` 帧数、`L` 采样点数、
`C` 通道数、`H` 头数。

**练习 T0**：给 `knnvc/matching.py` 的每一行加上形状注释，包括
`cosine_distance` 里的 `q`、`p`、返回值，以及 `knn_convert` 里 `best.indices`、
`best.values`、`pool[best.indices]`、`.mean(dim=1)` 的结果。
这是你自己写的代码，如果标注时有任何一处犹豫，就是这一关的价值所在。

## T1 · 索引、广播、花式索引（1~2 天，难度最高）

**要掌握的**

- 基础切片：`x[1:3]`、`x[..., -1]`、`x[:, :p_len]`
- 增删维度：`unsqueeze` / `squeeze` / `None` 索引 / `expand_dims`（numpy）
- 广播规则：**从最后一维向前对齐，维度相等或其中一个为 1 才能广播**
- `expand` 与 `repeat` 的区别（前者不复制内存，后者复制）
- 花式索引（integer array indexing）：用一个整数张量去索引另一个张量

**去哪读**：`rvc/infer/vc/pipeline.py` 第 186~189 行，faiss 检索那四行。
这是全仓库最浓缩的一处，一次用到了检索、花式索引、广播、按轴求和：

```python
# rvc/infer/vc/pipeline.py:186~189
score, ix = index.search(npy, k=8)
weight = np.square(1 / score)
weight /= weight.sum(axis=1, keepdims=True)
npy = np.sum(index_vectors[ix] * np.expand_dims(weight, axis=2), axis=1)
```

**练习 T1**（先手推，再写脚本验证）

1. 设 `npy` 形状 `[T, 768]`、`index_vectors` 形状 `[N, 768]`。
   依次写出 `ix`、`weight`、`weight.sum(axis=1, keepdims=True)`、
   `index_vectors[ix]`、`np.expand_dims(weight, axis=2)`、最终 `npy` 的形状。
2. 把 `keepdims=True` 去掉会怎样？是报错还是静默算错？**静默算错更可怕，
   想清楚为什么。**
3. `axis=2` 这个 `expand_dims` 如果写成 `axis=1`，广播会发生什么？
4. 用 `torch` 重写这四行，`np.expand_dims(weight, 2)` 换成 `weight[..., None]`，
   `np.sum(..., axis=1)` 换成 `.sum(dim=1)`，验证结果与 numpy 版本
   `np.allclose` 一致。
5. 对照你自己的 `knnvc/matching.py:51` 的 `pool[best.indices].mean(dim=1)`：
   **它和 RVC 这四行是同一个操作**，差别只在 RVC 用距离平方倒数做了加权平均，
   而你用的是等权平均。把你的实现改成加权平均，形状不许变。

**自检**：说出"广播"和"花式索引"各自在做什么，以及为什么
`index_vectors[ix]` 会凭空多出一个维度。

## T2 · permute / transpose / reshape / view / contiguous（1 天）

**要掌握的**

- `permute` 与 `transpose`：都只改 stride 不动内存，返回非连续张量
- `view` 要求内存连续，`reshape` 不要求（必要时自动 copy）
- 什么时候必须 `.contiguous()`
- **PyTorch 的两套维度约定**：卷积/插值要 `[B, C, T]`，
  Transformer/线性层要 `[B, T, C]`。RVC 代码里满地的 `permute` 全是在这两者间倒换

**去哪读**：`pipeline.py:198`

```python
# rvc/infer/vc/pipeline.py:198
feats = F.interpolate(feats.permute(0, 2, 1), scale_factor=2).permute(0, 2, 1)
```

**练习 T2**

1. 手推这一行三步的形状。为什么必须先 `permute` 再 `interpolate`，
   直接 `F.interpolate(feats, scale_factor=2)` 会插值到哪个维度上去？
2. **这一行的物理含义是什么？** `scale_factor=2` 把 50 帧/秒变成 100 帧/秒。
   为什么要变？（提示：查 `logs/Gu/config.json` 的 `hop_length` 和
   `sampling_rate`，算一下 NSF 声码器期待的帧率是多少）
3. 构造一个 `[2, 3, 4]` 张量，`permute(0,2,1)` 之后调 `.view(2, -1)`，
   记下报错信息全文。再换 `.reshape(2, -1)`，观察它能过。
   这个报错以后你会遇到很多次，现在就把它认熟。
4. `x.transpose(1, 2)` 和 `x.permute(0, 2, 1)` 对三维张量等价吗？四维呢？

## T3 · 布尔掩码与条件赋值（1 天）

**要掌握的**

- 布尔张量索引赋值：`x[x > 0] = 1`
- 掩码的顺序陷阱：连续两次条件赋值，第二次看到的是第一次改过的值
- `torch.where` 与掩码赋值的等价写法，以及为什么前者对 autograd 友好
- 用 `unsqueeze` 造出可广播的掩码

**去哪读**：三处，从易到难。

第一处 `pipeline.py:211~216`，清音保护：

```python
# rvc/infer/vc/pipeline.py:211~216
if protect < 0.5 and pitch is not None and pitchf is not None:
    pitchff = pitchf.clone()
    pitchff[pitchf > 0] = 1
    pitchff[pitchf < 1] = protect
    pitchff = pitchff.unsqueeze(-1)
    feats = feats * pitchff + feats0 * (1 - pitchff)
```

第二处 `pipeline.py:132~138`，F0 转梅尔刻度再量化到 256 档。

第三处 `rvc/infer/module/commons.py:131~135`，`sequence_mask`，
用两个 `unsqueeze` + 广播比较生成变长掩码，是这个模式的教科书写法：

```python
# rvc/infer/module/commons.py:131~135
def sequence_mask(length, max_length=None):
    if max_length is None:
        max_length = length.max()
    x = torch.arange(max_length, dtype=length.dtype, device=length.device)
    return x.unsqueeze(0) < length.unsqueeze(1)
```

**练习 T3**

1. `pitchff` 那段：注意第 213 行用 `pitchf` 判断而第 212 行改的是 `pitchff`。
   如果第 213 行误写成 `pitchff[pitchff < 1] = protect` 会怎样？
   （这就是"顺序陷阱"，`pitchff > 0` 已经被置成 1 了）
2. `pitchff` 的语义：`pitchf` 是连续 F0，等于 0 表示清音帧。
   所以浊音位置权重为 1、清音位置权重为 `protect`，
   最后一行是**在浊音帧用检索/插值后的特征、在清音帧混回原始特征**。
   用两句话写出"为什么清音帧要保护"，这是阶段 4 的伏笔。
3. `unsqueeze(-1)` 去掉会怎样？手推 `feats` 和 `pitchff` 的形状，
   说明广播为什么会失败（或者更糟：静默广播到错误的轴上）。
4. `sequence_mask`：设 `length = tensor([3, 5, 2])`，手写出返回的完整布尔矩阵，
   再跑代码核对。这个函数在 `models.py` 里到处被用来屏蔽 padding。
5. 把 `pitchff` 那两行条件赋值改写成一句 `torch.where`，验证结果一致。

## T4 · 矩阵乘法家族（半天）

**要掌握的**：`*`（逐元素）、`@` / `matmul`（矩阵乘）、`bmm`（批量）、
`einsum`（万能）。以及 `matmul` 对高维张量的广播规则。

**去哪读**：你自己的 `knnvc/matching.py:23~25`。

**练习 T4**

1. 用 `torch.einsum` 重写 `cosine_distance`，与原实现 `allclose`。
2. 用 `torch.cdist` 算欧氏距离版本，跑一次转换，听感和指标有什么变化？
   （顺便回答你注释里写的"为什么用余弦而非欧氏"是否真的成立——
   这是个可以做成正式消融的小实验）
3. `[B,T,D] @ [D,D]` 和 `[B,T,D] @ [B,D,T]` 分别得到什么形状？广播规则是什么？

## T5 · dtype / device / autograd 三条边界（1 天）

**要掌握的**

- `requires_grad` / `detach()` / `torch.no_grad()` / `.item()` 的区别与用途
- CPU↔GPU 搬运：`.to(device)`、`.cpu().numpy()`，以及
  "expected all tensors to be on the same device" 的排查
- fp16/fp32 混合：什么操作在 half 下会溢出成 `inf` 或 `nan`

**去哪读**

- `pipeline.py:182~196`：`feats[0].cpu().numpy()` → faiss（只吃 numpy float32）
  → `torch.from_numpy(npy).unsqueeze(0).to(self.device)`。
  **一次完整的 torch → numpy → torch 往返，是最常见的 bug 温床。**
- `pipeline.py:217`：`feats = feats.to(feats0.dtype)`，为什么需要这一句
- `rvc/train/train.py:487` 与 `506`：`with autocast(enabled=False)`，
  为什么算 loss 时要**关掉** fp16

**练习 T5**

1. 故意制造三个报错并记下完整信息：device 不一致、dtype 不一致、
   对 `requires_grad=True` 的张量调 `.numpy()`。
2. `x.detach()` 和 `with torch.no_grad()` 有什么区别？各自什么时候用？
3. 为什么 `mel` 损失和 `kl` 损失要在 `autocast(enabled=False)` 里算？
   （提示：`kl_loss` 里有 `exp` 和除法）

## T6 · Python 语言本身，面向"读得懂源码"（2~3 天）

只补读 RVC 必需的部分，按仓库里的出现频率排序。

| 语法点 | 去哪读 | 要能回答 |
|---|---|---|
| `nn.Module` 的 `__init__` / `forward` / `__call__` | `models.py:16` TextEncoder | 为什么调用 `self.enc_p(x)` 而不是 `self.enc_p.forward(x)` |
| 类继承与方法覆写 | `models.py:681` `SynthesizerTrnMs768NSFsid(SynthesizerTrnMs256NSFsid)` | 子类只改了什么？256 和 768 指的是什么 |
| `nn.ModuleList` vs 普通 list | `models.py:103`、`models.py:936` | 用普通 list 装子模块会出什么问题 |
| 上下文管理器 `with` | `train.py:459` autocast、`pipeline.py:168` no_grad | `with` 的进入/退出各做了什么 |
| 装饰器 | `@torch.no_grad()`、`@staticmethod` | 装饰器本质上是什么（一个返回函数的函数） |
| lambda 与闭包 | `pipeline.py:225` 那个传给 `run_cuda_graph` 的 lambda | 它捕获了哪些外部变量 |
| `*args` / `**kwargs` | `models.py` 各 Synthesizer 的构造函数 | 为什么 RVC 到处用 `**kwargs` 吞掉多余配置 |
| f-string 与格式化 | `matching.py:58`、`train.py:548` | `:.4f` / `:.3f` |
| `enumerate` / `zip` / 列表推导 | `train.py` 训练循环、`data_utils.py` | 列表推导与生成器表达式的区别 |
| 类型注解 | 你自己的 `knnvc/matching.py` 已经在用 | 注解会被运行时检查吗 |
| `is None` vs `isinstance(x, type(None))` | `pipeline.py:178~179` | 后者是啰嗦写法，为什么前者更好 |
| 路径与文件 IO | `rvc/tools/file_io.py`、`logs/Gu/filelist.txt` | Windows 路径转义、`os.path` vs `pathlib` |

**练习 T6**：写一个 `drills/t6_module.py`，从零定义一个 `nn.Module` 子类，
内部含 `nn.ModuleList`、一个 `forward`、一个用 `@torch.no_grad()` 装饰的
`infer` 方法，并打印 `list(model.parameters())` 的数量。
然后故意把 `ModuleList` 换成普通 `list`，观察参数数量变成 0 —— 这个坑值 1 天调试时间。

## 关卡与阶段的挂钩

| 关卡 | 什么时候做 | 为什么在这时 |
|---|---|---|
| T0 形状记账 | 立刻，阶段 0 之前 | 后面所有阅读都靠它 |
| T1 索引广播 | 阶段 0，做 `index_rate` 消融时 | 练习题就是 index_rate 那段代码 |
| T2 permute | 阶段 2 解剖 RVC 推理链时 | 链路上每一步都在 permute |
| T3 布尔掩码 | 阶段 2 末 / 阶段 4 初 | 直接通向 protect 和清音处理 |
| T4 矩阵乘 | 阶段 1 写探针时 | 探针本身就是一次矩阵乘 |
| T5 dtype/device/autograd | 阶段 1 写训练循环时 | 报错会自己找上门 |
| T6 Python 语言 | 阶段 2 与阶段 3 之间 | 读 `models.py` 前的最后准备 |

## T1/T2/T3 形状参考答案

**先自己推完再看。** 设 `T` 为 50Hz 下的帧数、`N` 为索引库向量数。

- T1：`ix` → `[T, 8]`；`score`/`weight` → `[T, 8]`；
  `weight.sum(axis=1, keepdims=True)` → `[T, 1]`；
  `index_vectors[ix]` → `[T, 8, 768]`；
  `np.expand_dims(weight, 2)` → `[T, 8, 1]`；
  相乘广播 → `[T, 8, 768]`；`sum(axis=1)` → `[T, 768]`。
  去掉 `keepdims` 后 `[T]` 与 `[T, 8]` 广播失败（8 ≠ T 时报错，
  而**当 T 恰好等于 8 时会静默算错**）。
- T2：`feats` `[1, T, 768]` → permute → `[1, 768, T]` → interpolate →
  `[1, 768, 2T]` → permute → `[1, 2T, 768]`。
- T3：`pitchf` `[1, T]` → `unsqueeze(-1)` → `[1, T, 1]`，
  与 `feats` `[1, T, 768]` 沿最后一维广播。
  `sequence_mask(tensor([3,5,2]))` → `[3, 5]` 布尔矩阵，
  第 0 行前 3 个 True，第 1 行全 True，第 2 行前 2 个 True。

---

# 阶段 0 · 把 knn-vc → RVC 的账算清

**目标**：填上 `项目总结.md` 第一节表格的空行和第八节的前两项，让前一段工作闭合。
**周期**：约 1 周。**配套语法关**：T0、T1。

## 实验设计

| 编号 | 实验 | 只变的那一个变量 | 产出 |
|---|---|---|---|
| 0-1 | RVC 重训 `if_f0=1` | `if_f0`，f0 方法 `rmvpe`，其余与 `Gu` 完全一致（40 epoch / batch 4 / `pretrained_v2` G40k） | 命名 `Gu_f0`，填表格最后一行 |
| 0-2 | `index_rate` 消融 | 0 / 0.25 / 0.5 / 0.75 / 1.0 | 5 行指标 + 一张曲线 |
| 0-3 | F0 后处理三组用 harvest 重测 | 测量工具统一 | 去掉第三节那个 ⚠️ |

**纪律**：`logs/Gu` 这次的配置（`if_f0=0`、40 epoch）已经从 `train.log` 第一行
确认过了。以后每个 RVC 实验都照 knn-vc 的规格写参数快照 JSON：

```json
{
  "model": "Gu_f0",
  "source": "data/source/complicateSentenceVocals.wav",
  "source_sha1": "...",
  "if_f0": 1, "f0_method": "rmvpe", "total_epoch": 40,
  "batch_size": 4, "pretrain": "pretrained_v2/f0G40k.pth",
  "index_rate": 0.75, "protect": 0.33, "f0_up_key": 0,
  "spk_completion": null, "f0_corr": null, "wer": null
}
```

WebUI 不会替你记这些，建议改走 `rvc/infer/cli.py` 命令行。

## 阶段 0 的核心阅读

`rvc/infer/vc/pipeline.py:175~198`。这段代码做的事是：ContentVec 特征拿去
faiss 索引里搜 `k=8` 近邻，按距离平方倒数加权平均，然后
`index_rate * 检索特征 + (1 - index_rate) * 原始特征`。

**这就是你手写的 `knn_convert`**，差别只有三点：topk 固定为 8、用了加权平均、
多了一个线性插值旋钮。所以 `index_rate` 消融是把两个方法正面连起来的实验：
`index_rate=1` 趋近纯检索（你的 knn-vc），`index_rate=0` 是纯学习映射。

你在 knn-vc 上得到的"池子越大最优 topk 越小"这条结论，在这里能否复现？

## 自检问题

- `index_rate` 从 0 调到 1，音色和内容分别往哪个方向走？为什么？
- RVC 的 faiss 索引和你的特征池，存的是同一种东西吗？（`3_feature768` 目录）
- 为什么 RVC 用距离加权而你用等权平均？哪个更合理？

## 预测 / 实际 / 教训

| 编号 | 预测（做之前写） | 实际 | 教训 |
|---|---|---|---|
| 0-1 | | | |
| 0-2 | | | |
| 0-3 | | | |

---

# 阶段 1 · 机器学习基础，用"层探针"当载体

**目标**：把 `学习记录.md` 第 37 行标记的空洞（dropout / BatchNorm /
反向传播 / batch）从"能背结论"变成"手推过、跑过、踩过坑"。
**周期**：1~2 周。**配套语法关**：T4、T5。

不做 MNIST。做一个跟你课题直接相关、又刚好覆盖全部基础概念的任务。

## 实验 1-1：WavLM 逐层说话人探针

你手里有 `essays/layerWise.pdf`，也有"浅层含说话人信息、中间层音素最纯"这个
从论文读来的结论（`学习记录.md` 第 28 行还留着写反了的版本）。现在用数字验证它。

- **数据**：你自己的录音帧 vs LJSpeech 帧，WavLM 第 1~24 层各抽一份特征
- **任务**：二分类"这一帧是谁说的"
- **模型**：先 logistic regression（一层线性 + sigmoid），再加隐层变 MLP
- **产出**：`explore/04_layer_probe.py` + 一张 24 点折线图（横轴层号、纵轴验证准确率）

这个实验会强迫你亲手过一遍下面每一项：

| 概念 | 在这个实验里怎么碰到 |
|---|---|
| batch / epoch / 学习率 | 数据几万帧必须分 batch；调 lr 看 loss 是震荡还是收敛 |
| 反向传播 | **先用 numpy 手写 logistic regression 的梯度（三行公式），再换 autograd 对比数值一致。这一步别跳** |
| train/val 划分 | 同一句话的相邻帧高度相关，按帧随机切分会让准确率虚高。**故意先踩一次，再改成按句子切分** |
| 过拟合 | 参数量大的 MLP 在小数据上训练准确率 99%、验证 60% 是什么样 |
| Dropout / BatchNorm | 加进 MLP，观察 `model.train()` 与 `model.eval()` 下同一输入输出不同 |
| 优化器 | SGD → Adam → AdamW 各跑一次。`train.py:227` 用的就是 AdamW，`betas=(0.8, 0.99)` |
| 损失函数 | BCE 与交叉熵的关系；为什么分类不用 MSE |

## 自检问题

- 说话人信息的峰值在第几层？跨过第几层后掉到接近 50%（随机水平）？
- 按帧切分和按句子切分，准确率差多少？为什么这是**数据泄漏**？
- `model.eval()` 具体改变了哪几类层的行为？（你在总结里写过三个例子，
  现在要能指出探针模型里哪一行受影响）
- AdamW 的 `weight_decay` 和在 loss 里加 L2 惩罚，是同一件事吗？

## 预测 / 实际 / 教训

| 编号 | 预测 | 实际 | 教训 |
|---|---|---|---|
| 1-1 峰值层号 | | | |
| 1-1 按帧 vs 按句准确率差 | | | |

---

# 阶段 2 · RVC 解剖，复刻你对 knn-vc 做过的事

**目标**：写 `explore/05_rvc_anatomy.py`，在推理链路上逐级打印张量形状，
做到对整条链路的每一步形状变化都能不查代码说出来。
**周期**：1~2 周。**配套语法关**：T2、T3、T6。

## 对应关系表（自己填完再核对代码）

| knn-vc（你已懂） | RVC | 去哪看 |
|---|---|---|
| WavLM-Large 第 6 层，1024 维 | ContentVec（HuBERT-base 结构）最后一层，768 维 | `infer/hubert.py`、`logs/Gu/3_feature768` |
| 50 帧/秒（步长 320 @16k） | 50 帧/秒 → `interpolate(scale_factor=2)` → 100 帧/秒 | `pipeline.py:198` |
| HiFi-GAN 上采样 [10,8,2,2] = 320 | NSF-HiFiGAN 上采样 [10,10,2,2] = **400** = `hop_length` @40k = 10ms | `logs/Gu/config.json` |
| 无 F0 | F0 → 梅尔刻度 → 量化 256 档（`f0_coarse`）→ `nn.Embedding(256, 192)` | `pipeline.py:132~138`、`models.py:40` |
| 特征直接送声码器 | 特征 → TextEncoder(6 层 Transformer) → Flow 逆变换 → NSF 声码器 | `models.py:530~737` |

## 自检问题（先猜再验证，不许先查）

1. 为什么 F0 要先转梅尔刻度再均匀量化，而不是直接按 Hz 均匀量化？
   （和你第一课"log 域做音高运算"是同一件事）
2. 为什么 ContentVec 取最后一层，而 knn-vc 取第 6 层？两个模型的训练目标差在哪？
   （关键词你自己写在总结第九节：`ContentVec speaker disentanglement`）
3. `config.json` 里 `spk_embed_dim: 109` 是什么？你训练时 `sid=0`，
   另外 108 个说话人嵌入去哪了？这关系到底模在做什么。
4. `protect` 保护的到底是什么？（T3 练习 2 已经铺过路）
5. `n_mel_channels: 125` —— 为什么不是 knn-vc 那边熟悉的 80？
6. `segment_size: 12800` 是什么单位？除以 `hop_length=400` 等于多少帧？
   这个数字决定了训练时每步看多长的音频。

## 同期任务：把 Gu 的 loss 曲线看懂

开 TensorBoard 读 `logs/Gu/events.out.tfevents.*`，看四条曲线：
`loss/g/mel`、`loss/g/kl`、`loss/g/fm`、`loss/d/total`。

`train.py:524` 那一行是整个 VITS 的目标函数：

```python
# rvc/train/train.py:524
loss_gen_all = loss_gen + loss_fm + loss_mel + loss_kl
```

这一阶段不要求解释它，只要求**看清四条曲线的形状并截图存档**。
阶段 3 会回来逐项解释。

## 预测 / 实际 / 教训

| 编号 | 预测 | 实际 | 教训 |
|---|---|---|---|
| 2-1 链路形状推导 | | | |

---

# 阶段 3 · VITS 的数学，三块分别推一遍

**目标**：能对着 `train.py:524` 那一行，把四项 loss 分别对应到 VAE / GAN / GAN / VAE，
并说清 `c_mel=45`、`c_kl=1` 这两个权重为什么差 45 倍。
**周期**：2~3 周，全程最重的一段。**配套语法关**：T6 收尾。

每一块都走"读公式 → 做玩具实验 → 回代码找对应行"三步。

## 3a · VAE 与 ELBO

- **读**：Kingma & Welling 的 VAE 原文第 2 节，或 Doersch 的《Tutorial on VAE》
- **推**：写出 `ELBO = 重构项 − KL 项`；理解"一对多"（同样内容对应无数合法波形）
  为什么需要隐变量；重参数化技巧手推一次梯度
- **玩具**：在 80 维梅尔帧上训一个小 VAE，观察重构结果**模糊**。
  这直接对应你已经理解的"L1 回归得到平均化结果"，也就是 GAN 项存在的动机
- **回代码**：`models.py:136` `PosteriorEncoder` 就是 q(z|谱)，输出 `m, logs`；
  `train.py:521` 的 `kl_loss(z_p, logs_q, m_p, logs_p, z_mask)` 就是那个 KL 项

## 3b · Normalizing Flow

- **读**：RealNVP（affine coupling）一篇足够，Glow 选读。核心只有一句：
  **可逆变换 + 雅可比行列式好算 ⇒ 概率密度可精确计算 ⇒ KL 可直接优化**
- **玩具**：2D 双月数据集上手写 4 层 affine coupling flow，把高斯变成双月并画出来。
  `log_det` 那一项必须自己写，不许调库
- **回代码**：`models.py:82` `ResidualCouplingBlock`，`n_flows=4`。
  注意训练时 `flow(z)` 正向、推理时 `flow(z_p, reverse=True)` 逆向 ——
  **这就是 VITS 为什么非要可逆结构**

## 3c · GAN 的训练动态

你已经懂 MPD/MSD 的结构和动机（总结第七节），缺的是训练过程本身。

- **读**：`rvc/train/losses.py` 全文（很短），LSGAN 损失、feature matching 损失
- **回代码**：`train.py:506~526`，一个 step 里 D 先更新再 G 更新。
  `GradScaler` + `autocast` 是 fp16 混合精度，`train.log` 第一行
  "精度 torch.float16" 就是它
- **观察**：Gu 的 TensorBoard 里 `loss/d/total` 和 `loss/g/total` 是否在某区间拉锯

## 自检问题

- 四项 loss 各自在优化什么？去掉任意一项会坏在哪里？
- `c_mel=45` 而 `c_kl=1`，这个 45 倍说明设计者更在意什么？
- **如果 D 的 loss 掉到接近 0，意味着什么？** 这时 G 还能学到东西吗？
- feature matching 损失为什么能稳定 GAN 训练？
- `train.py:541~544` 把 `loss_mel > 75` 和 `loss_kl > 9` 截断 ——
  这是在改梯度还是只改日志？（看清它在哪个位置）

## 预测 / 实际 / 教训

| 编号 | 预测 | 实际 | 教训 |
|---|---|---|---|
| 3a 小 VAE 重构效果 | | | |
| 3b flow 玩具是否收敛 | | | |
| 3c D-loss 趋势 | | | |

---

# 阶段 4 · F0 与 NSF，把第一课的源-滤波器模型走到底

**目标**：亲手听到"声源"和"声道"在神经网络里的字面实现。
**周期**：约 1 周。**配套语法关**：T3 应用。

## 实验 4-1：F0 估计器横评

pyin / harvest / rmvpe / fcpe（后两个仓库里都有权重和代码），
在同一段源音频上跑，四条 F0 曲线叠在一张图上。

你在 `项目总结.md` 第三节标注的那个约 0.15 的"工具偏差"，到底哪个工具更接近真值？
**用一段已知音高的合成正弦波（或 vibrato 扫频）做 ground truth**，这样才有裁判。

## 实验 4-2：把 NSF 的激励信号单独抽出来听

读 `models.py:267` 的 `SineGen` 和 `models.py:346` 的 `SourceModuleHnNSF`。
它按 F0 生成基频 + 谐波的正弦叠加，清音段换成噪声。

**把这个激励信号抽出来存成 wav 播放**：你会听到一个嗡嗡的蜂鸣。
这就是第一课"声源激励"的字面实现，声码器剩下的全部工作就是给它套上"声道"。

## 实验 4-3：Gu vs Gu_f0 的结构差异定位

两者的唯一结构差异是：TextEncoder 多了 `emb_pitch`（`models.py:40`、`models.py:61`），
Generator 换成 `GeneratorNSF`（`models.py:403`）。
阶段 0 测到的全部指标差异都来自这两处。在代码里逐行确认这句话是否成立。

## 自检问题

- NSF 把 F0 做成条件输入，和你第三课 WORLD 后处理的做法，本质差别是什么？
  （你已经付过 0.126 的音色税，现在要能用代码指出钱花在哪）
- `SineGen` 为什么要生成谐波而不只是基频正弦？
- 清音段为什么必须换成噪声？（和 T3 的 `protect` 是同一个问题的两个面）
- `f0_up_key`（升降调）在 pipeline 的哪一步生效？它改的是 `f0_coarse` 还是 `pitchf`，
  还是两者都改？

## 预测 / 实际 / 教训

| 编号 | 预测 | 实际 | 教训 |
|---|---|---|---|
| 4-1 哪个 F0 工具最准 | | | |
| 4-2 激励信号听起来像什么 | | | |

---

# 阶段 5 · 训练动态实验，拆开"架构 + 预训练 + 数据"

**目标**：回答你自己在 `项目总结.md` 第一节提出的那个诚实问题 ——
RVC 的 0.917 里，架构、预训练底模、数据各占多少功劳？
**周期**：约 2 周，主要是 GPU 时间。

| 编号 | 实验 | 变量 | 要回答的问题 |
|---|---|---|---|
| 5-1 | epoch 消融 | 40 / 100 / 200 / 400，每 20 epoch 存权重 | 会不会过拟合？过拟合的 VC 模型听起来是什么样？ |
| 5-2 | **从零训练 vs 底模微调** | 不加载 `pretrained_v2`，其余全同 | 13 分钟数据从零训能否到 0.7？如果不能，说明 knn-vc 的 0.71 其实不差 |
| 5-3 | 数据量 | 3 分钟 / 13 分钟 / 40 分钟（LJSpeech 有 24 小时） | 微调模式下的饱和点，和 knn-vc 的 5~10 分钟一致吗？ |
| 5-4 | batch_size | 4 / 8 / 16 | 显存与收敛速度的权衡；lr 是否要随 batch 同步调 |

**5-2 是这一阶段的核心**，它是唯一能把"架构贡献"和"预训练贡献"分开的实验。

**产出**：一张能直接放进 `项目总结.md` 第一节的新表格。

## 自检问题

- 一个过拟合的 VC 模型，在客观指标和听感上分别有什么特征？
- 为什么 `epochs` 配置写着 20000 而实际只训 40 就停？
  （看 `train.py` 里 `total_epoch` 与 `hps.train.epochs` 的关系）
- 5-3 如果饱和点和 knn-vc 一致，说明什么？如果差很多，又说明什么？

## 预测 / 实际 / 教训

| 编号 | 预测 | 实际 | 教训 |
|---|---|---|---|
| 5-1 | | | |
| 5-2 | | | |
| 5-3 | | | |
| 5-4 | | | |

---

# 阶段 6 · 回到最初的目标

换动漫角色干音，做一个能拿出手的成品。这一阶段处理的是真实数据管线，
仓库里已经有全部工具：

- `assets/pymss_weights/` 里的 BS-RoFormer 做人声分离、
  `dereverb_mel_band_roformer` 去混响 —— 这解决 README 里说的
  "带 BGM / 混响重" 的素材问题
- 推理参数 `f0_up_key`（跨性别转换时 ±12 半音）、`protect`、`rms_mix_rate`、
  `index_rate` **全部用阶段 0~4 建立的理解去设置，不许照抄默认值**
- 数据清洗沿用 knn-vc 时期的经验（README"三步走"那一节）：
  剔除带 BGM、战斗音效、重混响、喊叫哭腔的片段

**伦理约束（沿用 `项目总结.md` 第十节）**：只使用本人声音或已获公开授权的素材；
产出注明 AI 合成；不发布可能被误认为真人发言的音频。

---

# 之后往哪走

阶段 6 完成后你会亲身感到 RVC 的短板：**每个目标说话人都要单独训练一个模型**。

下一代方向是 **zero-shot VC**（FreeVC / seed-vc / CosyVoice 这一系），
核心从"为每个说话人拟合一个映射"变成"把说话人编码成一个向量当条件"。
以及 **扩散模型路线**（DDSP-SVC / Diff-SVC）。

这两条线的前置知识 —— 说话人编码器（你在 `eval/metrics.py` 里用的 ECAPA 就是一个）
和条件生成 —— 在阶段 1 和阶段 3 都会碰到。**现在不要碰**，基础没扎实之前
它们只会又变成一个黑盒。

---

# 全程纪律

1. 实验前写预测，实验后填"实际"和"教训"。
2. 每个 RVC 实验都有参数快照 JSON，格式沿用 knn-vc 那套。
3. **模型命名带变量**：`Gu`、`Gu_f0`、`Gu_f0_scratch`、`Gu_f0_e200`。
   不许再出现两个只有你自己记得区别的模型。
4. 每写一行 tensor 操作就跟一句形状注释（T0）。这条是本次新增的硬性要求。
5. 每阶段结束写一段"我能回答的问题 / 我还不能回答的问题"，
   最低线就是各阶段的自检问题列表。
6. `项目总结.md` 只放干净结论，脏实验留在 `学习记录.md`，两份都不删。

# 进度勾选

- [ ] T0 形状记账
- [ ] 阶段 0 · knn-vc → RVC 闭合（0-1 / 0-2 / 0-3）
- [ ] T1 索引与广播
- [ ] 阶段 1 · ML 基础与层探针
- [ ] T4 矩阵乘 · T5 dtype/device/autograd
- [ ] 阶段 2 · RVC 解剖
- [ ] T2 permute · T3 布尔掩码 · T6 Python 语言
- [ ] 阶段 3 · VITS 数学（3a VAE / 3b Flow / 3c GAN）
- [ ] 阶段 4 · F0 与 NSF
- [ ] 阶段 5 · 训练动态与归因
- [ ] 阶段 6 · 成品
